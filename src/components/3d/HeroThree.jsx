import { styles } from "./styles";
import ComputersCanvas from "./Computers";

const HeroThree = () => {
  return (
    <section className={`relative w-full h-screen max-sm:h-[400px] mx-auto`}>

      <ComputersCanvas />

    
    </section>
  );
};

export default HeroThree;
